<div class='breadcrumbs'>
  <%= link "Brands >", to: brand_path(@conn, :index) %>
  <%= link "#{@brand.name}", to: catalog_phone_path(@conn, :index, @brand.id) %>
</div>

<h2>New phone</h2>

<%= render "form.html", changeset: @changeset,
                        brand: @brand,
                        brands: nil,
                        series: @series,
                        action: catalog_phone_path(@conn, :create, @brand.id) %>
