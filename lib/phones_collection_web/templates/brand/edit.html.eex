<div class='breadcrumbs'>
  <%= link "Brands >", to: Routes.brand_path(@conn, :index) %>
  <%= link "#{@brand.name} >", to: Routes.catalog_phone_path(@conn, :index, @brand) %>
  <%= "Edit" %>
</div>

<%= render "form.html", changeset: @changeset,
                        action: Routes.brand_path(@conn, :update, @brand) %>
